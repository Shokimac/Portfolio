<div class="signin">


  <div class="signin__section">
    <h1>ログイン</h1>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class='signin__error'>
    <% flash.each do |message_type, message| %>
    <%= content_tag :div, message, class: message_type %>
    <% end %>
  </div>

  <div class="signin__nav">
    <p>
      いとをかしに登録している方は、下記からログインしてください。<br>
      まだ登録していない方は <%= link_to "こちらから登録", new_registration_path(resource_name) %> してください。
    </p>
  </div>

  <div class="input">
    <%= f.label :email, class:"input__label" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"input__form" %>
  </div>

  <div class="input">
    <%= f.label :password, class:"input__label" %><br />
    <%= f.password_field :password, autocomplete: "current-password", class:"input__form" %>
  </div>

  <% if devise_mapping.rememberable? %>
  <%= f.check_box :remember_me %>
  <%= f.label :remember_me %><br>
  <% end %>

  <%= f.submit "ログイン", class:"signin__button" %>
  <% end %>

  <div class="signin__nav--google">
    <p>Googleアカウントをお持ちの方はこちらから</p>
  </div>

  <%=  link_to user_google_oauth2_omniauth_authorize_path do %>
  <%= image_tag 'google.png', size:"200x50" , class:"signin__google" %>
  <% end %>

</div>