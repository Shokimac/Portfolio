<div class="confirmationProfile">
    <div class="confirmationProfile__section">
        <h1>プロフィール作成画面</h1>
    </div>

    <div class="confirmationProfile__nav">
        <p>
            よろしければ、あなたのプロフィールを作成ください。<br>
            このまま、先へ進むこともできます。<br>
            ※この画面は初回ログイン時のみ表示されます。
        </p>
    </div>

    <%= form_with model: @user, url: user_path(@user.id), local: true do |f| %>
    <%= render 'layouts/error_message', model: f.object %>

    <div class="input">
        <%= f.label :image, "プロフィール画像", class:"input__label" %><br>
        <%= image_tag @image_url, size:"400x300", id:"img1" %><br>
        <%= f.attachment_field :image, id:"myfile" %>
    </div>

    <div class="input">
        <%= f.label :name, "ユーザー名", class:"input__label" %><br>
        <%= f.text_field :name, class:"input__form"  %><br>
    </div>

    <%= f.label :email, "メールアドレス", class:"confirmationProfile__label" %><br>
    <div class="confirmationProfile__email">
        <%= @user.email %>
    </div>

    <div class="input">
        <%= f.label :introduction, "自己紹介文", class:"input__label" %><br>
        <%= f.text_area :introduction, class:"input__form--area" %>
    </div>

    <%= f.submit "ようこそ、いとをかしへ", class: "confirmationProfile__button" %><br>
    <% end %>
</div>