<div class="inquiry">

<%= form_with model:@inquiry, url:inquiries_path, method: :post, local: true do |f| %>

<%= f.label :name, "お名前" %>
<%= f.text_field :name %>

<%= f.label :message, "お問い合わせ内容" %>
<%= f.text_area :message %>

<%= f.submit "送信" %>

<% end %>